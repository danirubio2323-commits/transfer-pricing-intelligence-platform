# Mínimos verificados, no pines exactos.
#
# Los pines originales (pydantic==2.5.0, numpy==1.26.2, streamlit==1.28.1) eran
# de 2023 y quedaron por detrás del entorno donde se ejecuta la suite. Un
# proyecto que declara una versión y prueba con otra acaba fallando solo en la
# máquina de quien lo clona. Se declara el suelo real y CI fija el techo al
# instalar lo más reciente compatible.

# Núcleo
pydantic>=2.9

# Interfaz
streamlit>=1.36

# Cálculo
numpy>=1.26

# Informes
reportlab>=4.0

# IA (opcional en ejecución: sin clave, la capa se desactiva)
anthropic>=0.40

# Utilidades
python-dotenv>=1.0.0

# Pruebas
pytest>=7.4
pypdf>=5.0
